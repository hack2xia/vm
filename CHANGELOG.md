# Changelog

## [0.1.3] - 2026-09-05

防御性加固版本：`vmrun list` 输出格式校验、`VMRUN_BIN` 非法覆盖语义收口。

### 新增
- `vm version`：显示版本，stdout 纯净可捕获，配合 `vm doctor` 输出构成报障现场信息；`vm doctor` 顶部增加版本行。
- `vmrun list` 输出格式校验：`rc=0` 但缺少 `Total running VMs:` 表头（假/不兼容 vmrun、协议变化、输出截断）不再当成「零台运行」——`vm status` 显式报错、`vm delete` 拒绝删除（运行保护宁可不删不误判）、`vm doctor` 判 ✗。

### 变更
- `VMRUN_BIN` 非法覆盖语义收口：裸名（非绝对路径）可从 PATH 解析出可执行文件时自动规范化为绝对路径（doctor 显示与实际执行严格一致）；不存在/不可执行的值警告后按原值保留、调用必然失败（126/127）——与旧版差别是不再笼统「警告后按原值使用」，且任何情况下都不回退默认解析，避免误配置/测试注入失效时静默落到真实 vmrun。
- 测试新增：`vmrun list` 输出异常（缺表头）时 `status` 失败/`delete` 拒绝、`VMRUN_BIN` 裸名规范化与解析失败不静默回退。断言总数 187。
- CI 加固：声明 `permissions: contents: read`（最小权限）、job `timeout-minutes`、`concurrency` 取消同分支旧运行；语法检查覆盖 `_vm` 与 `tests/*.zsh` 全部文件；push 触发范围收敛到 `main` 与 `v*` 标签。
- README 新增「故障排查」章节：短名冲突、清单陈旧、`vmrun list` 失败、`vmrun 不可用`（`VMRUN_BIN`）、Tools 未装影响面、外部 VM 删除风险。

## [0.1.2] - 2026-09-04

安全边界版本：vmrun 依赖显式化、输出安全集中化、外部 VM 删除风险分级、测试隔离 fail-closed。

### 新增
- `VMRUN_BIN` 环境变量：唯一实际执行的 `vmrun`。默认解析为 Fusion 内的绝对路径（Fusion 不在默认位置时从 PATH 解析出绝对路径，仍找不到则保持 `vmrun` 由调用报错）；所有调用统一走 `_vmrun` 包装器，不再依赖 PATH 优先级，同名函数/其他 `vmrun` 不可能被意外执行。`vm doctor` 显示的即真正会执行的路径。
- `vm delete --allow-external`：删除 `VM_DIR` 之外的 VM（inventory-only / 外部路径）时，`--yes` 单独不足以静默删除，必须同时加 `--allow-external`——否则自动化里一条 `vm delete xxx --yes` 就能删掉清单指向的任意外部 VM。交互式流程不变（外部 VM 仍二次确认，`--allow-external` 不豁免名称确认）。
- 冲突 UX 闭环：`VM_CONFLICT` 从布尔改为冲突路径列表。破坏性命令被拒时展示保留路径、被隐藏的全部冲突路径与解决指引；`vm vms` 在冲突短名下追加冲突路径；`vm doctor` 列出完整冲突集合。此前用户知道冲突却看不到完整冲突集合，无从解决。
- 动态内容统一经 `_vm_esc` 安全化：`%` 转义防 prompt 展开，控制字符可见化（ESC→`^[`、CR→`^M`、LF→`\n`）防终端注入伪造显示。此前散落各调用点的手工 `%` 转义有多处遗漏（冲突提示、delete 确认、doctor、清单状态列等），现集中于数据侧，补齐全部遗漏点。
- 回显命令参数按 shell quoting 逐个表示（`${(q)}`）：含空格的路径在回显中可原样复制执行，「看到的就是执行的」名实相符。
- `zpty` 真实交互删除测试：正确输入确认后删除（断言 `deleteVM` 确实执行、`.vmx` 消失）、错误输入/EOF（Ctrl-D）取消、外部 VM 二次确认、运行状态最后一刻拦截（不调用 `deleteVM`）；并对 TTY（有色渲染、`%F{…}` 名字按字面显示）与非 TTY（无色、控制字符可见化）两种输出模式分别断言。测试基建上规避了 `zpty -r` 的两个陷阱：不带 pattern 是非阻塞快照（竞态漏读），带 pattern 阻塞到匹配（永不匹配时永久挂起）——采用「预写输入 + 无条件哨兵输出」保证读取必然命中，测试不可能挂死。

### 修复
- `vmrun` 解析不稳定：此前 Fusion 目录追加到 PATH 尾部后裸调 `vmrun`，PATH 前部的同名程序或同名 zsh 函数会抢先执行（doctor 诊断与实际执行可能不一致）。
- 测试隔离 fail-closed：fake `vmrun` 的创建与 `chmod` 此前未检查结果，磁盘/权限异常时可能经 PATH 回退命中真实 `vmrun`。现改为 `mktemp` 随机沙盒（原 `.sandbox.$$` 路径可预测）、全部夹具写入 `|| die`、清理注册到 EXIT/INT/TERM/HUP、fake 经 `VMRUN_BIN` 显式注入（不依赖 PATH 优先级）、fake 对沙盒外的写入/删除路径 fail-closed（拒绝执行并退出 99）。

### 变更
- 测试基建：`vmrun 缺失`/`doctor vmrun 缺失`/符号链接路径/doctor 台数等用例改用 `VMRUN_BIN` 注入（此前靠 PATH 前置或函数遮蔽，正是被本次修复消除的隐患）；新增用例：外部 VM `--yes` 拒绝与 `--allow-external` 成功、冲突路径展示（拒绝提示/`vms`/`doctor`）、控制字符可见化、回显 quoting、`zpty` 交互删除全流程。断言总数 176。

## [0.1.1] - 2026-09-04

安全收口版本：完成 VM 身份识别与破坏性操作模型的收口，并新增环境体检。

### 新增
- `vm doctor`：只读环境体检。检查 `vmrun`/Fusion 可用性与版本、`VM_DIR`/`VM_INVENTORY` 有效性、真正探活一次 `vmrun list`（此前安装验证用的 `vm vms` 不调用 `vmrun`，证明不了环境完整），并报告扫描数量与冲突短名；有 ✗ 项退出码为 1，可直接用于脚本判断。

### 修复
- `vm doctor` 运行台数恒显示 1：取行用错了下标语义——`(I)` 返回匹配行号（表头恒为 1）而非行内容，且嵌套 `${${(f)x}[(r)…]}` 会退化成标量下标返回首字符；改为先落中间数组再以 `(r)` 取行。新增 3 台探针的回归测试（1 台的沙盒夹具行号与台数恰好重合，测不出此 bug）。
- P0：`vm status` / `vm delete` 的运行判定改按文件身份（设备+inode）与 `vmrun list` 比对。此前用小写路径字符串比较——清单保存符号链接路径、`vmrun list` 回显真实路径时，运行中的 VM 会被误判为「未运行」，`vm delete --yes` 的运行保护可被绕过。`delete` 现于交互确认之后、执行 `deleteVM` 之前最后一刻复查运行状态，`vmrun list` 失败视为无法确认、拒绝删除。
- 默认目录内大小写同名（仅大小写敏感卷可出现）的两个 bundle 现在进入冲突状态：此前只警告跳过，破坏性命令不拒绝。
- 冲突短名的保护扩大到全部状态变更命令：`up` / `suspend` / `pause` / `unpause` / `snap create` 与作为 `clone` 源，此前仅覆盖 `down` / `kill` / `reset` / `snap delete` / `snap revert` / `delete`。
- 无色输出不再经 `print -P` 渲染：颜色剥除与 `%%` 转义相互作用时，含 `%F{…}` 的名称曾被吃得只剩片段（如 `%F{red}foo` 输出成 `oo`）；现按字面输出，动态内容原样呈现。同时补齐未知 VM / 未知子命令 / 未知选项等处缺失的 `%` 转义。
- `VM_DIR` / `VM_INVENTORY` 在 source 时规范化为绝对路径：相对路径覆盖不再生成随 `cd` 失效的扫描缓存。
- `vm clone`：新名拒绝以 `-` 开头（否则产生 `delete` 选项解析无法处理的名字）；目标目录改用不带 `-p` 的 `mkdir` 原子占位，仅在本命令亲手创建时才做失败清理，消除检查/创建/清理之间的竞态。
- `vm delete` 支持 `--` 透传：磁盘上手工创建的 `-` 开头名字的 VM 仍可正常管理。
- `vm ip -- <name>`：`--` 此前会被当作透传参数之一留在参数列表里，误报「参数过多」；现在被正确消费，`--` 之后的名字（含 `-` 开头的）可正常解析。
- `vm status` / `vm vms` 列对齐改按屏幕显示宽度计算（`_vm_dispwidth`，宽字符按 2 列）：CJK 名称不再顶偏后续列；同时修正「先转义 `%` 再补齐」的顺序错误（含 `%` 的名字曾因此少补一格）。
- 补全：`up` 等单参数命令只在第一个参数位补 VM 名，与严格参数契约一致（此前任意位置都补）。

### 变更
- 测试基建：fake `vmrun` 的未知子命令改为显式失败（补齐 `start` / `suspend` / `pause` / `unpause` / `reset` 桩），杜绝测试自身掩盖 `vmrun` 拼写错误；夹具创建失败立即终止，绝不退回 PATH 中可能存在的真实 `vmrun`。
- 新增回归测试：符号链接路径身份（status 识别 + delete 拦截）、冲突短名拒绝状态变更、默认目录内大小写同名（仅大小写敏感卷可仿真，否则自动跳过）、相对 `VM_DIR` 规范化、含 `%F{…}` 名称的无色输出、clone 拒绝 `-` 开头新名、`delete --` 透传、`vm doctor`（完整环境/探活台数/vmrun 缺失）。断言总数 145。
- 测试脚本输出改走 `_vm_p`：管道 / CI 中不再泄漏 ANSI 转义，支持 `NO_COLOR`；描述文本中的 `%` 统一转义。
- 新增补全行为测试（桩捕获 `compadd` / `_values` / `_describe` / `_alternative` 实参）、`--` 透传回归、CJK 列宽对齐断言。

## [0.1.0] - 2026-09-03

首个可发布版本。定位明确为「VMware Fusion 轻量级生命周期管理工具」而非 `vmrun` 全功能封装。

### 新增
- `vm delete <name> [--yes]`：永久删除整台 VM。运行中 / 短名冲突 / `.vmx` 失效时拒绝；交互式必须输入短名确认，位于 `VM_DIR` 之外的 inventory-only VM 需二次确认；自动化必须显式 `--yes`。
- 每个子命令的严格参数个数校验：多余参数与拼写错误不再被静默忽略，报错并给出用法。
- 非终端（管道 / 重定向 / 命令替换）自动关闭颜色；支持 `NO_COLOR`。
- CI：GitHub Actions 在 macOS 上执行 `zsh -n` 与沙盒回归测试。

### 修复
- P0：磁盘扫描结果被清单「首条」同名条目静默覆盖（可能操作错 VM）。同名合并改为按文件身份（设备 + inode）判定：同一台用清单路径刷新，不同物理文件保留先出现者并标记冲突。
- 冲突短名的破坏性命令（`down` / `kill` / `reset` / `snap delete` / `snap revert` / `delete`）一律拒绝执行。
- `vm status` 检查 `vmrun list` 退出码：查询失败时显式报错并透传非零退出码，不再把查询失败误报为「全部未运行」。
- inventory 条目在合并前统一验证（绝对路径 / `.vmx` 后缀 / 存在可读），失效路径（含 inventory-only 陈旧项）不进管理列表。
- `vm clone`：`full + snapshot` 组合显式拒绝（此前会静默忽略）；克隆失败时清理本次创建的目标目录，可立即重试。

### 变更
- 移除 `vm ssh` 子命令（参数解析复杂度已超安全阈值，原生 SSH 足够）。
- 测试基建：fake `vmrun` 改为逐参数记录 argv（`|` 分隔）；契约断言前清空调用日志；补多组回归测试。

## [0.1.0 之前]

- 手动发现（Fusion 清单 + 默认目录兜底）、短名解析、电源管理、`vm ip`、快照备注（`.vmsd`）、克隆、zsh 补全。
